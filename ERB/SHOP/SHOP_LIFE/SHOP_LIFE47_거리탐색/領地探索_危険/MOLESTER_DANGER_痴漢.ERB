;-------------------
;존재 판정
;굳이 시키지 않는 것
;-------------------
@TERRITORY_EVENT_MOLESTER_DANGER_EXISTS()
#DIM 대상
RETURN 1

;-------------------
;발생 판정
;-------------------
@TERRITORY_EVENT_MOLESTER_DANGER_DECISION(대상)
#DIM 대상
RETURN 1

;-------------------
;본체
;-------------------
@TERRITORY_EVENT_MOLESTER_DANGER(대상)
#DIM 대상

IF IS_MALE(대상)
	PRINTFORML 떠들썩한 인파 속에서, 치한을 당하고 있는 여성을 발견했다.
	PRINTFORML 부끄러운 것인지 얼굴이 빨개져서 저항하지 못하고 당하고 있다.
	PRINTFORML
	CALL ASK_MULTI("도와준다", "내버려 둔다")
	SELECTCASE RESULT
		CASE 0
			PRINTFORML 내버려 둘 수는 없다.
			PRINTFORMW %조사처리(ANAME(대상),"는")% 인파를 헤치고 여성과 치한 사이에 끼어들었다.
			PRINTFORMW 여성으로부터 감사와 답례를 받았다.
			CALL ICPRINT("금<3000>을 받았다", "L", 칼라_주의)
			MONEY += 3000
		CASE 1
			PRINTFORML 번거롭게 참견하고 싶지는 않다.
			PRINTFORML 내버려 두었다.
	ENDSELECT
	RETURN 1
ENDIF

PRINTFORML 떠들썩한 인파 속을 걷고 있는데 치한에게 습격당했다.
PRINTFORMW 기분 탓인가 싶었지만, %STR_BODY("엉덩이:감촉", 대상)%를 쓰다듬어졌다.
PRINTFORML 몸을 비틀어 피하려 했지만 딱 달라붙어 떨어지지 않는다.
PRINTFORML 

CALL ASK_MULTI("소리를 지른다", "얌전히 있는다", "똑같이 돌려준다")

SELECTCASE RESULT
	CASE 0
		PRINTFORML %조사처리(ANAME(대상),"는")% 크게 소리쳤다.
		PRINTFORMW 주위에 사정을 설명하려던 순간, 치한이 도망쳐 버렸다.
		PRINTFORML 치한을 퇴치하는데는 성공했지만, 자신을 바라보던 놈의 천박한 시선은 잊을 수 없었다.
		CALL FUCK(대상, "욕망, 노출")
	CASE 1
		PRINTFORML 가만히 있으면 곧 질리겠지
		PRINTFORMW 그렇게 생각한 %조사처리(ANAME(대상),"는")% 얌전히 있기로 했다.
		PRINTFORML 
		PRINTFORML %조사처리(ANAME(대상),"가")% 저항하지 않는 것을 좋아하는 것이라고 생각한 치한의 행위가 과감해졌다.
		PRINTFORMW 처음에는 옷 너머로 %STR_BODY("엉덩이:감촉", 대상)%를 쓰다듬던 것이, 옷 안으로, 속옷 안으로 들어가 직접 만졌다.
		PRINTFORML 그러고도 질리지 않았는지 %STR_BODY("가슴:애무", 대상)%를 주무르고 %STR_BODY("클리", 대상)%를 손가락 끝으로 괴롭혔다.
		PRINTFORMW 끈적한 손놀림에 %조사처리(ANAME(대상),"는")% 점차 달콤한 숨결을 내뱉기 시작했다.
		PRINTFORMW 쐐기를 박듯 %STR_BODY("질:처녀 확인", 대상)%의 얕은 부분을 손가락 끝으로 휘젓자, %조사처리(ANAME(대상),"는")% 목소리를 억누르며 절정했다.
		CALL FUCK(대상, "욕망, Ｃ, Ｖ, Ｂ, 노출, 마조")
		PRINTFORML 
		PRINTFORML ……그 뒤로도 농락당한 %조사처리(ANAME(대상),"는")%, 치한에게 붙들려 골목으로 끌려갔다.
		PRINTFORMW 저항하려고 해도 이미 허리가 풀려버려, 살짝 성감대를 스친 것 만으로 힘이 빠져 버렸다.
		PRINTFORML 질척질척해져 있던 속옷을 빼앗기고, 엉덩이를 내미는 듯한 자세로 음부를 공개됐다.
		PRINTFORMW 그는 %ANAME(대상)%의 방금 전 치태에 대해 놀리듯 희롱하며 페니스를 삽입해 왔다.
		PRINTFORML 그의 페니스는 손가락 이상으로 능숙해, 곧 %조사처리(ANAME(대상),"는")% 비부에서 애액을 뿜어내며 절정했다.
		PRINTFORMW 질육을 쑤셔지면서 %STR_BODY("유두:욕정", 대상)%와 %STR_BODY("클리", 대상)%를 꼬집히자, %조사처리(ANAME(대상),"는")% 칠칠치 못한 암컷의 교성을 내질렀다.
		PRINTFORMW 이미 %ANAME(대상)%에게 반항심 따위는 한 톨도 남지 않아, 단지 그에게 농락당할 뿐이었다.
		PRINTFORMW 치한의 마음에 든 %조사처리(ANAME(대상),"는")% 그 후에도 충분히 조교되어 암컷의 기쁨을 몸에 새겼다.
		CALL FUCK(대상, "욕망, 봉사, 정애, 성기술, 성교, Ｃ, Ｖ, Ｂ, Ｖ성교", "처녀상실, 질내사정", GET_SPERM_ID("치한"), @"치한의 \@RAND:2 ? 페니스 # 입술\@", "치한", "", "뒷골목에서의 강간")
		PRINTFORML 
		PRINTFORML %조사처리(ANAME(대상),"는")% 새벽녘이 되어서야 겨우 풀려났다.
		PRINTFORMW 젖꼭지를 꼿꼿하게 발기시키고 애액을 질질 흘리며, 꿈을 꾸는 듯한 기분으로 귀가길에 올랐다.
	CASE 2
		PRINTFORML 이쪽을 기분 좋게 해주겠다면, 저쪽도 기분 좋게 해줘야지
		PRINTFORMW 그렇게 생각하며 치한의 사타구니를 만지고, 옷 위로 페니스를 쓰다듬었다.
		PRINTFORML 그는 깜짝 놀란 것 같았지만, 곧 %ANAME(대상)%의 의도를 이해한 것 같다.
		PRINTFORMW 옷 안쪽, 속옷 안에까지 손을 넣어 %STR_BODY("엉덩이:감촉", 대상)%와 %STR_BODY("가슴:애무", 대상)%、%STR_BODY("클리", 대상)%를 가지고 장난쳤다.
		PRINTFORMW 그 끈적끈적한 손놀림에 %조사처리(ANAME(대상),"는")% 달콤한 목소리를 흘리며, 쾌감에 대한 답례로 그의 페니스를 훑기 시작했다.
		PRINTFORML 그는 그것만으로는 만족할 수 없었던 듯, %ANAME(대상)%의 허벅지에 페니스를 찔러왔다.
		PRINTFORMW %조사처리(ANAME(대상),"는")% 번잡한 인파 속에서 꼼짝도 하지 못하는 척 하며, 그의 양물을 허벅지와 %STR_BODY("음순:처녀 확인", 대상)%로 끼우고 문질러주었다……
		CALL FUCK(대상, "욕망, Ｃ, Ｖ, Ｂ, 성기술, 성교, 봉사, 노출, 정애")
		PRINTFORML 
		PRINTFORMW 그 후, 남자는 %ANAME(대상)%의 귓가에 속삭이듯 호텔로 초대했다.
		PRINTFORMW %조사처리(ANAME(대상),"가")% 고개를 끄덕이자, 그는 연인처럼 허리에 팔을 둘러왔다.
		PRINTFORMW 그는 손가락뿐 아니라 허리놀림도 능숙해서, 밤새 몇 번이나 귀여움받았다.
		CALL FUCK(대상, "욕망, 봉사, 정애, 성기술, 성교, Ｃ, Ｖ, Ｂ, Ｍ, 키스, 펠라티오, Ｖ성교", "처녀상실, 질내사정, 키스상실, 구강사정", GET_SPERM_ID("치한"), @"치한의 \@RAND:2 ? 페니스 # 입술\@", "치한", "", "호텔에서의 화간")
		PRINTFORML 
		PRINTFORMW 듬뿍 조교당한 %조사처리(ANAME(대상),"는")% 그와 입을 맞추고 헤어졌다.


RETURN 1