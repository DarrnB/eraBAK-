;-------------------
;존재 판정
;굳이 시키지 않는 것
;-------------------
@TERRITORY_EVENT_BOOKSHOP_EXISTS()
#DIM 대상
RETURN 1

;-------------------
;발생 판정
;-------------------
@TERRITORY_EVENT_BOOKSHOP_DECISION(대상)
#DIM 대상
RETURN 1

;-------------------
;본체
;-------------------
@TERRITORY_EVENT_BOOKSHOP(대상)
#DIM 대상

PRINTFORML 낡은 책방이 눈에 띄어 들어가 보았다.
PRINTFORML 안에는 중년의 점주가 홀로 카운터에 앉아 책을 읽고 있었다.
PRINTFORMW 그는 이쪽을 한 번 쳐다보더니 다시 시선을 책으로 돌렸다.
PRINTL
PRINTFORML 어떻게 할까？
CALL ASK_MULTI("병술서를 읽는다" ,"요리책을 읽는다" ,"마술서를 읽는다")
IF RESULT == 0
	PRINTFORMW 병술서를 읽어 보았다.
	PRINTFORML 
	PRINTFORMW 꽤 도움이 되었다.
	CALL PRINT_ADD_EXP(대상, "지략경험치", RAND:8 + 6, 1)
ELSEIF RESULT == 1
	PRINTFORMW 요리책을 읽어 보았다.
	PRINTFORML 
	PRINTFORMW 새로운 레시피를 익혔다.
	CALL PRINT_ADD_EXP(대상, "요리경험치", RAND:8 + 6, 1)
ELSEIF RESULT == 2
	PRINTFORMW 마술서에 손을 뻗었다.
	PRINTFORML 
	IF TALENT:대상:신비지식 == 1
		PRINTFORMW 마술의 요령을 배웠다.
		CALL PRINT_ADD_EXP(대상, "신비경험치", RAND:8 + 6, 1)
	ELSE
		PRINTFORMW 그러나 무슨 말을 하는 건지 이해하지 못했다.
	ENDIF
ENDIF
IF RAND:2 || IS_MALE(대상)
	RETURN
ENDIF
PRINTFORML 
PRINTFORML %조사처리(ANAME(대상),"가")% 책을 읽던 와중 점차 머리가 멍해졌다.
PRINTFORML 뭔가 이상하다고 느낄 새도 없이, %조사처리(ANAME(대상),"는")% 넋을 잃고 그 자리에 서 있었다.
PRINTFORMW 그 모습을 보고 있던 점주는 피식 웃더니, %ANAME(대상)%에게 다가갔다……
PRINTFORML 
PRINTFORML %조사처리(ANAME(대상),"는")% 책방의 점원으로 일하고 있다.
PRINTFORML 접객, 책 정리, 발주에 더해 펠라치오, 섹스도 점장의 명령대로 의욕적으로 해나갔다.
PRINTFORMW 그의 최면술에 걸린 %조사처리(ANAME(대상),"는")% 인식과 상식을 개변되어 그것을 당연하게 여기게 되었다.
PRINTFORML 손님이 없을 떄는 책장 뒤에서 범해지고, 손님이 오면 카운터 밑에 숨어 봉사했다.
PRINTFORML 물론 모든 정액을 흘리지 않고 받아들이고, 봉사 후에는 감사의 마음을 담은 딥쓰로트도 잊지 않았다.
PRINTFORML 가게 문을 닫은 후에도 집으로 돌아가지 않고 그의 시중을 들면서 몇 번이고 몇 번이고 씨받이 섹스를 반복했다.
PRINTFORMW 그러나 이것들은 모두 지금의 %ANAME(대상)%에게는 평범한 일이었기에, 그의 아이를 낳으라는 명령을 받자 쿵쿵 울리는 자궁을 쓰다듬으며 고개를 끄덕였다……
CALL FUCK(대상, "욕망, 봉사, 정애, 성기술, 성교, Ｃ, Ｖ, Ｂ, Ｍ, 키스, 펠라티오, Ｖ성교", "처녀상실, 질내사정, 키스상실, 구강사정", GET_SPERM_ID("책방의 점주"), @"책방의 점주의 \@RAND:2 ? 페니스 # 입술\@", "책방의 점주", "", "최면을 걸쳐지면서 조교")
CALL FUCK(대상, "욕망, 봉사, 정애, 성기술, 성교, Ｃ, Ｖ, Ｂ, Ｍ, 키스, 펠라티오, Ｖ성교", "처녀상실, 질내사정, 키스상실, 구강사정", GET_SPERM_ID("책방의 점주"), @"책방의 점주의 \@RAND:2 ? 페니스 # 입술\@", "책방의 점주", "", "최면을 걸쳐지면서 조교")
PRINTFORML 
PRINTFORML 하지만 이런 생활도 오래 가진 못했고, 어느 순간 최면이 풀렸다.
PRINTFORMW %조사처리(ANAME(대상),"는")% 혼란스러워하면서도, 그에게 다시 잡히기 전에 가게에서 도망쳤다.

RETURN 1